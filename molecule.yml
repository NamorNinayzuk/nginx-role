---
dependency:
  name: galaxy
  enabled: false
driver:
  name: docker
platforms:
  - name: instance
    image: docker.io/pycontribs/debian:10
    pre_build_image: true
provisioner:
  name: ansible
verifier:
  name: ansible
